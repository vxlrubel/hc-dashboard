# Feature Catalogue

This file lists the ERP feature scope by module.

## Authentication & Profile

Login, logout, password reset, MFA-ready profile management.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Dashboard

Statistics, charts, recent activity, revenue and payroll summaries.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Users

System user accounts connected to employees, clients and funders.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Roles

Role management using Spatie Permission.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Permissions

Fine-grained module permissions.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Branches

Branch/company location data and branch-level isolation.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Employees

Staff records, branch assignment, employment profile and status.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Employee Documents

Staff documents such as passport, certificate, contracts and CV.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Clients

Client profiles, care plan links, staff assignment and funder data.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Care Plans

Client care plan summaries, goals, risk notes and review cycle.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Funders

Funding organisations and payment responsibility.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Ratesheets

Hourly, weekend, holiday and night rate definitions.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Rotas

Named rota templates and effective schedule ranges.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Shift Templates

Reusable shift definitions for recurring scheduling.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Recurring Rotas

Pattern based rota generation.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Holiday Calendar

Public/company holidays used in payroll and scheduling.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Schedules

Planned visits/shifts linking employee, client, branch and rota.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Activities

Actual work activities and visit records.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Timesheets

Approved worked hours generated from activities/schedules.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Leaves

Employee leave requests and approvals.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Forms

Dynamic form builder, submissions, publishing and assignment.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Media Manager

Media upload, attachment, file library and document tagging.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Medicine Management

Client medicines, dosage, administration logs and review dates.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Invoices

Client/funder invoices, status tracking and invoice items.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Invoice Payments

Invoice payments, refunds, credit notes and outstanding balances.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Salary Management

Employee salary generation, overtime and deductions.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Reports

Operational, payroll, branch, client and finance reports.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Export Center

PDF, Excel and CSV exports.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Messages

Private staff/client conversations and read receipts.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Notifications

Database, email and push notifications.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Activity Logs

Audit trail for create, update, delete, restore and auth events.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Settings

Company, invoice, SMS/email, timezone, currency and default rates.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## System Configuration

Maintenance, cache, queues, jobs and application runtime settings.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Trash & Restore

Central soft-delete restore and permanent delete workflow.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Global Search

Cross-module search with saved filters.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Bulk Actions

Bulk delete, restore, export, assign and status changes.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## Health Check

Operational health endpoints for deployment monitoring.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.

## OpenAPI / Scramble

API documentation generation using Scramble.

Core features:

- List, detail, create, update and delete where applicable.
- Search, filter, sort and pagination.
- Export where useful.
- Restore and force delete when the module uses soft deletes.
- Activity logging for important actions.
- Policy and permission checks.


## Additional enterprise features

### Employee

- Emergency contacts
- Bank accounts
- Payroll history
- Performance reviews
- Training records
- Certifications
- Availability calendar
- Clock in/out history

### Client

- Emergency contacts
- Medical history
- Allergies
- Risk assessments
- Care notes
- Incidents
- Visit history
- Attachments

### Scheduling

- Shift swapping
- Open shifts
- Overtime tracking
- Automatic shift assignment
- Conflict detection

### Finance

- Expenses
- Payment history
- Refunds
- Credit notes
- Tax management

### Reporting

- KPI dashboard
- Revenue analytics
- Payroll analytics
- Staff performance
- Client statistics
- Branch performance
- Attendance reports

### System

- Backup management
- Audit log
- Queue monitor
- Failed jobs
- Cache management
- Scheduled jobs
- Application settings
- Localization
- Timezone management
